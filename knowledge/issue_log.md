# KT-Assist — Issue Log

## Active Issues / Features
*(none — all closed)*
| A7 | Windows hotfixes: colon-in-cache-filename OSError + CP1252 UnicodeDecodeError on `read_text()` | Claude | Fixed |

## Closed Issues History

6. **Ruling A3 — At-risk predicate (KT Program level, Executive Dashboard). CORRECTION:** my initial proposal (this entry, prior version) was superseded on inspection — `services/reporting/executive_dashboard_service.py` already implements and tests this predicate, marked `[PROPOSAL, ratified]` in its own docstring, predating this session. **Actual shipped predicate:** `at_risk = readiness != "Ready" AND (program mean coverage < config.COVERAGE_SUFFICIENCY_THRESHOLD OR any package has a CompetencyResult with is_critical=True and score < config.CRITICAL_COMPETENCY_GATE_THRESHOLD OR any package has an open GapRecord with risk_level="High")`. No code change needed; A3 was already closed. Lesson: verify shipped code before ruling on something the issue log lists as open — the log can lag actual implementation.

7. **Ruling A4 — Competency warning band (Screen 8 Pass/Fail/Warning indicator). CORRECTION:** my initial proposal (flat 70–79 Warning / ≥80 Pass for all competencies) was superseded on inspection — `services/reporting/readiness_dashboard_service.py` already implements this, marked `[PROPOSAL, ratified]`, predating this session, and it's asymmetric (smarter than my proposal): **critical** competencies are `Fail < 70`, `Warning 70–74` (5-point band, `_WARNING_BAND = 5`), `Pass ≥ 75`; **non-critical** competencies have no Fail state (they don't gate readiness) — `Warning < 70`, `Pass ≥ 70`. No code change needed; A4 was already closed.

8. **Ruling A5 — D8 pillar scoring method. CONFIRMED, no correction needed:** my proposal (weighted intra-pillar per S26) matches the already-shipped implementation in `services/agents/kase_scoring.py`, verified by `tests/test_session26_kase_scoring.py` (`test_aggregate_pillar_scores_matches_worked_example`, which hand-computes the weighted formula and asserts against it). The Chunk 6 Stage-5 simple-average is confirmed illustrative only. No code change needed.

**Note on A3/A4/A5 generally:** all three "open rulings" turned out to already be implemented, tested, and marked `[PROPOSAL, ratified]` in code — the issue log was stale relative to the actual codebase. Going forward, verify shipped code before treating an issue-log item as open.

13. **IN PROGRESS (handoff, not committed/pushed) — Hierarchical demo replay-proof, offline phase.** Working in `/tmp/kt_assist_check` on `demo-mode` @ `1eff195`. New, uncommitted, local-only files (a fresh clone/session will NOT have these until pushed):
    - `services/demo/hierarchical_fixtures.py` — pinned demo IDs (Ruling A: fixed program/package/3-receiver UUIDs, kept out of `config/`).
    - `services/demo/hierarchical_kai_attributes.py` — pilot-profile (`System`/`Known Issue`/`Task`) structured attribute overlay, values derived mechanically from each object's own existing description in `scripts/seed_demo_kai_cache.py` (no invented facts). Also currently contains `SYSTEM_DEPENDS_ON_EDGES` — **superseded, see bug below, do not use as-is.**
    - `services/demo/hierarchical_gap_answers.py` — the 7 evidence-confirmation closure answers (one per Known Issue's withheld `validation_status`/`evidence_refs`), keyed by `(object_id, rule_family)` — confirmed this is the exact same key `services/coverage/consolidation.py` already uses as `KnowledgeGap` identity. Explicit `UnknownGapSignatureError` for unregistered signatures (no silent fabrication). This part is verified working.
    - `services/demo/receiver_strategies.py` — loads the 3 golden `competency_response_strategy` dicts from `datasets/golden/golden_responses.json` (no duplication), with an empty `TUNING_OVERRIDES` layer for Ruling B.
    - `scripts/seed_demo_hierarchical_kai_cache.py` — writes the pilot-suffixed KAI cache (`:pilot-v1` key, confirmed required — see Step-1 finding below) by importing/overlaying onto `scripts/seed_demo_kai_cache.py`'s existing object graph (no duplication). **Currently also injects `SYSTEM_DEPENDS_ON_EDGES` into the shared relationships cache — this needs to be REMOVED per the bug fix below.**
    - `scripts/run_hierarchical_demo_replay_proof.py` — the end-to-end offline proof runner (ingest_hierarchical → validate_hierarchical → closure loop → persist → KAR → generate_assessment → 3 receivers via `build_scenario_responses`/`score_and_persist_readiness`, using a `SimpleNamespace(sufficiency_gate_passed=...)` stand-in for legacy `CoverageResult` and `kar_adapter.adapt_kar_to_gates()` to bridge KAR into the unmodified `score_and_persist_readiness`). **Verified working through the ingest + initial validate steps; fails at the closure step due to the bug below.**

    **Step 1 finding (confirmed, needed the new cache):** `services/agents/kai_extraction._chunk_cache_key()` appends a `:pilot-v{N}` suffix whenever `pilot_object_types` is non-empty, so the original `seed_demo_kai_cache.py` cache entries are NOT hit by `ingest_hierarchical()` — a fresh, separately-keyed pilot cache (with `attributes` populated on System/Known Issue/Task) was required. Relationship-discovery's cache key (`{hash}:relationships`) does NOT fork on pilot mode, so that one file is legitimately shared/extended, not duplicated.

    **Real bug found (not yet fixed, needs a ruling):** `services/agents/kai_relationship_discovery.py::discover_relationships()`'s type-pair acceptance check (`expected_pair = RELATIONSHIP_TYPE_RULES.get(rel.relationship_type)`) only consults the **primary** `RELATIONSHIP_TYPE_RULES` dict, never `RELATIONSHIP_TYPE_RULES_ADDITIONAL` (`schemas/knowledge_graph.py`). So a `System → Dependency` `DEPENDS_ON` proposal — the exact case the "KASE/hierarchical relationship compatibility patch" (main, commit `c1cd851`) added `RELATIONSHIP_TYPE_RULES_ADDITIONAL` to support — is silently rejected as a "type-pair mismatch" during initial KAI relationship discovery, even though `services/coverage/dimensional_scoring.py`'s RC computation and `RELATIONSHIP_TYPE_RULES_ADDITIONAL` itself correctly treat it as valid. Confirmed empirically: seeded 62 relationships (56 original + 6 System→Dependency) into the shared cache; only 56 were persisted after a real `ingest_hierarchical()` run; RC computed as 0.0; 5 RELATIONSHIP_GAP findings fired, blocking the Sufficiency Gate. This is a real, pre-existing gap in the Wave-7 compatibility patch (affects any real Claude-live run too, not just this demo), independent of the demo work.
    - **Decision (workaround, not yet implemented):** do NOT patch `discover_relationships()` in this offline-replay-proof phase (out of scope per this phase's constraints — no lifecycle/logic changes). Instead: stop seeding `SYSTEM_DEPENDS_ON_EDGES` via the initial KAI cache at all; let each System start with an open `RELATIONSHIP_GAP` (rule_family `failure_recovery`), and close it through the **hierarchical closure loop's fixture answer** using `InterpretedRelationshipChange` (`action="create"`, matched by `source_name`/`target_name`) via `services/graph/graph_update.py::apply_interpreted_changes()` — confirmed by reading that function (lines ~125-148) that its relationship-creation path performs **no** type-pair check at all, only a by-name existence check. This proves real closure without touching the buggy code, and is arguably a better demo beat (a genuine KGE-closed relationship gap) than a pre-populated one.
    - **This bug should be reported to Sharath as a separate, real architecture finding** (fix candidate: change `discover_relationships()`'s check to use `schemas.knowledge_graph.all_valid_pairs_for()`/`is_valid_relationship_pair()` instead of the raw `RELATIONSHIP_TYPE_RULES` dict lookup) — independent of whether the demo workaround above is adopted.

    **Diagnostic-only numbers observed** (will change once the relationship-gap workaround is applied — do not treat as final): initial `validate_hierarchical()` on the real 50-object/56-relationship pilot-attributed graph: TC=1.0, AC=1.0, RC=0.0 (the bug above), OS=1.0, EV=0.0 (7 Known Issues' evidence deliberately withheld, as designed), KCS=0.75, KQS=0.7, Sufficiency Gate=False, Quality Gate=False, 5 critical_unresolved_gaps, 2 transition_risks.

    **Remaining steps (not started):** (a) remove `SYSTEM_DEPENDS_ON_EDGES` from the KAI-cache seed path; (b) add a relationship-closure answer set (parallel to the existing evidence-confirmation one) to `hierarchical_gap_answers.py`, built with `InterpretedRelationshipChange`; (c) rerun `run_hierarchical_demo_replay_proof.py` end to end; (d) steps 7-12 of the original task (KAR from closed graph, real `generate_assessment`, 3 receivers' real KASE/KRA outcomes, fixture tuning if needed per Ruling B, tests per the 10-item list, final report). None of steps 7-12 have been executed yet.

    **State/environment note:** repo clone lives at `/tmp/kt_assist_check` (container-local, not guaranteed to persist across sessions). Baseline before this work: `demo-mode` @ `1eff195`, full suite 676 passed/1 skipped, confirmed clean. All new files above are **uncommitted and unpushed** as of this entry — a new chat/session will need either (1) this same container/path if it persists, or (2) the files recreated from scratch using the detail above, or (3) whatever was pushed after this entry was written (check `git log demo-mode` / a `demo-mode-hierarchical-wip` branch for a possible WIP commit made right after this log entry).

9. **A2 — Phase 12 unified invariants suite, closed 2026-07-04.** Added the two missing guards to `tests/invariants/test_architectural_boundaries.py`: (a) Phase 9 number-guard, re-exercised independently against a minimal `ExplanationData` fixture — confirms the narrative layer discards any Claude output with an untraceable number and falls back to deterministic template text; (b) Phase 10 aggregate-don't-re-score guard, re-implemented as a grep over the 3 dashboard service files for the 4 forbidden scoring-formula constants (`OIS_WEIGHTS`, `CRITICALITY_WEIGHTS`, `EVIDENCE_SCORES`, `OBJECT_VALIDATION_SCORES`). Both new tests pass; full suite run at 518 passed, 1 skipped (no regressions). All three guards (Phase 9 number-guard, Phase 10 no-rescore, Phase 11 HTTP-only import) now live in one file per the Phase 12 goal; original per-phase test files kept as-is (belt-and-suspenders, fuller worked examples).

10. **A7 — Windows environment hotfixes, closed 2026-07-04.** Two Windows-only bugs surfaced by local test runs: (a) `OSError` in `services/core/claude_client.py::_cache_path()` — cache keys containing `:` produced invalid Windows filenames; fixed by replacing `:` → `-` before building the path. (b) `UnicodeDecodeError` from `Path.read_text()` defaulting to CP1252 on Windows instead of UTF-8 — fixed by passing `encoding="utf-8"` explicitly at 4 call sites: `test_architectural_boundaries.py::_top_level_imported_modules`, `::_code_level_claude_references` (same bug, not in the original report, fixed for consistency), `::test_dashboard_services_never_reconstruct_scoring_formulas`, and `test_frontend_boundary.py::_imported_top_level_modules`. Full suite re-verified: 518 passed, 1 skipped.

11. **A1 — Phase 13 datasets D1–D3+D8, found already complete 2026-07-04 (not new work).** `datasets/power_bi_dashboard/` already has all of D1 (source doc + core assets), D2 (`intentional_gaps.json` — Known Issue omitted, System/Control/Escalation partial, measured initial coverage 71.05%), D3 (`gap_answers.json` + `coverage_curve.json` — 2 scripted steps reaching 86.84%, deliberately left short of 100% since Escalation/Known Issue stay unresolved). D8 (`golden/golden_responses.json`) has all 3 required outcomes (Ready/Not Ready/Conditionally Ready) measured against the real KASE pipeline. `tests/datasets/` — 8/8 passing. Scope note already in `manifest.json`: Dataset 2 (D4–D6) and Dataset 3 (D7, spec-marked optional) are explicitly deferred/out-of-scope for this build pass, not missing work. Note: this dataset (71.05%→86.84%) is independent of DemoRunner's separate scripted demo scenario (63%→89%, OIS 84) — two different fixtures, not a conflict; `demo_runner.py` does not reference this dataset.

12. **A6 — Repo hygiene, closed 2026-07-04.** Untracked ~1,400 previously-committed runtime artifacts that predated their `.gitignore` rules: all of `assets/<uuid>/` (test-upload files; kept the 4 brand logo files tracked), `data/kt_assist.db.bak`, `data/graphs_smoketest/`, `data/tmp_cache_dbg/`, and 21 `data/.fuse_hidden*` NFS/FUSE artifacts (found during cleanup, same category, already `.gitignore`d but never actually untracked). Removed 3 dead test files: `tests/zz_debug_test.py` and `tests/zz_debug_test2.py` (debug wrappers that just re-invoked `test_demo_runner`'s real test under an alias — no unique coverage) and `tests/test_demo_runner_copy.py` (already an empty placeholder). Removed 3 empty stub files/packages: `agents/__init__.py`, `storage/__init__.py`, `models/__init__placeholder__.py` (confirmed unused — no code imports `agents` or `storage` anywhere). Full suite re-verified after each step: 517 passed, 1 skipped (down from 518 — the 2 removed debug-wrapper tests, expected).
1. **Bug #1 — lifecycle_state desync** (commit `ed3019d`). Executive Dashboard showed "Draft/Not Assessed" after full pipeline because `DemoRunner.run_all()` never advanced program lifecycle. **Fix:** lifecycle now advances **through HTTP endpoints** (not direct service mutation), keeping DemoRunner faithful to real user flow. Files: `services/orchestration/demo_runner.py`, routers, Screen 1. **Why:** HTTP-only guard means the demo must exercise the same path users do.

2. **`domain_breakdown_json` always null in Validation Center.** No code path persisted `CoverageResult` from a live `run_kva()`. **Fix:** created `services/coverage/coverage_persistence.py` as a **leaf shared module** (breaks `graph_update` ↔ `workflow_runner` circular import) and extended the upload endpoint to run `validate()` + `persist_coverage_result()` after `ingest()`. **Why leaf module:** embedding persistence in either orchestration or graph_update reintroduced the cycle.

3. **Stale `graph_version_id` in DemoRunner.** After gap closure advanced graph v1→v2, DemoRunner kept passing v1, corrupting downstream reads. **Fix:** re-read current version post-mutation. Established convention: never cache graph version across mutations. Files: `services/orchestration/demo_runner.py`, `services/graph/graph_update.py`.

4. **`close_gap()` computed but never persisted `KVAResult`.** Every gap response recomputed coverage then discarded it. **Fix:** persist via `coverage_persistence` inside `services/coverage/gap_governance.py`. Discovered (like #2) only by **chaining HTTP calls end-to-end** — codified as a testing principle.

5. **Structural refactor + spec reconciliation.** Flat `services/` → 8 domain subpackages (`a111da7`), dead flat duplicates removed (`469671c`), `config.py` monolith → `config/` package (`bf9e929`); orphaned shadowed `config.py` and duplicate root `frameworks/`/`prompts/` removed 2026-07-04. Spec side (via Cowork): competency catalog reconciled to **12 OIF competencies / 4 pillars** with correct weights + critical flags; KTTL template profiles fixed to exact required/optional object sets per package type. **Why:** Master Spec v2 is the reconciliation authority; code follows spec, never the reverse.
