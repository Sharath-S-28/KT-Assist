# KT-Assist — Issue Log

## Active Issues / Features
| #24 | Research / IP / publication track — provenance & IP ownership gate OPEN (blocks all publication and independent-build activity) | Sharath | Open |
| A7 | Windows hotfixes: colon-in-cache-filename OSError + CP1252 UnicodeDecodeError on `read_text()` | Claude | Fixed |

## Closed Issues History

24. **Research / IP / publication track opened — 2026-08-14, OPEN.** New persistent registry `knowledge/research_program.md` (novelty matrix, prior-art matrix, claim-evidence log, decision log, paper manifest, open-questions register) — created because these registries cannot survive across chat sessions otherwise. Phase 0 (reconstruction) complete: canonical system model, evolution map (v1 legacy -> Phase 12 invariants -> Waves 1-7 -> #14/#16 instrumentation findings -> #17-#22 demo/UX/portability -> #23 Artifact initiative), and a 9-mechanism inventory (M1-M9) with preliminary confidentiality classification. **No novelty claim has been made and none may be made until a novelty-matrix row exists.** Two blocking findings recorded: (a) **Provenance/IP gate is OPEN** — the system was built in the context of a pitch to Genpact (employer) and the owner now intends to develop it independently; employment/invention-assignment terms, build time/equipment, pitch documentation trail, and the confidentiality class of `KCTA_KT_Transcript_PBI_Dashboards.docx` are all UNKNOWN, and all publication/independent-build activity is held behind this gate pending Indian IP/employment counsel review. (b) **Evidence base currently supports zero empirical papers** — every receiver response to date is Claude- or fixture-authored (E3), OIS has no external validation target (E1), n=1 domain (E2), the v1-vs-hierarchical ablation has never been run despite both implementations existing (E4), and live-model extraction reliability is unverified (E5). No code changed this session; docs-only.

23. **Artifact Master Blueprint authored — closed 2026-07-15.** Produced `KT_Assist_Artifact_Master_Blueprint.docx` (25 pp, delivered as download only per ruling — deliberately NOT committed): single source of truth for rebuilding KT Assist as a fully live Claude Artifact (React, window.storage shared multi-project workspace, Claude API for extraction/interpretation/evidence/narrative only). **Sharath rulings captured this session:** (a) Artifact = fully live tool (upload any transcript), not demo replay; (b) blueprint format = .docx download only; (c) branding = Genpact-aligned palette + own KT Assist logo ("Continuity Knot" concept, Funnel Sans); (d) Knowledge Chat = READ-ONLY Q&A over validated graph (deterministic lexical+graph-neighborhood retrieval, object-id citations, validation badges; unanswerables → candidate-gap queue for Reviewer); (e) graph updates only via structured flows (gap closure / edit forms / evidence confirmation, human-approved, version+revalidate); (f) multi-project shared workspace; (g) cardinal scoring rule reaffirmed — blueprint specifies a Deterministic Scoring Kernel, [PROPOSAL R1] Pyodide running the actual Python scoring modules verbatim in-browser (TS port w/ golden-parity fallback), Claude never scores. Blueprint consolidates Waves 1–7 + demo/guided-UX state + issues #1–#22; carries known open items into §9 (discover_relationships additional-pairs gap, validate persist asymmetry, KS/AT competencies, coarse evidence states, live-prompt reliability = top risk w/ hardening plan) and registers 9 open rulings in Appendix E (R1 kernel, R2 Quality-Gate threshold, R3 full KTTL v2 profiles, R4 freshness windows, R5 version retention, R6 sampled assessment, R7 evidence scale, R8 KS/AT mechanisms, R9 confidentiality posture). Golden parity fixtures (Priya 85.0 Ready/Silver; 73.535 Conditionally Ready; 71.7 Not Ready; PBI KAR before/after) frozen as the kernel acceptance contract. No code changed this session.

6. **Ruling A3 — At-risk predicate (KT Program level, Executive Dashboard). CORRECTION:** my initial proposal (this entry, prior version) was superseded on inspection — `services/reporting/executive_dashboard_service.py` already implements and tests this predicate, marked `[PROPOSAL, ratified]` in its own docstring, predating this session. **Actual shipped predicate:** `at_risk = readiness != "Ready" AND (program mean coverage < config.COVERAGE_SUFFICIENCY_THRESHOLD OR any package has a CompetencyResult with is_critical=True and score < config.CRITICAL_COMPETENCY_GATE_THRESHOLD OR any package has an open GapRecord with risk_level="High")`. No code change needed; A3 was already closed. Lesson: verify shipped code before ruling on something the issue log lists as open — the log can lag actual implementation.

7. **Ruling A4 — Competency warning band (Screen 8 Pass/Fail/Warning indicator). CORRECTION:** my initial proposal (flat 70–79 Warning / ≥80 Pass for all competencies) was superseded on inspection — `services/reporting/readiness_dashboard_service.py` already implements this, marked `[PROPOSAL, ratified]`, predating this session, and it's asymmetric (smarter than my proposal): **critical** competencies are `Fail < 70`, `Warning 70–74` (5-point band, `_WARNING_BAND = 5`), `Pass ≥ 75`; **non-critical** competencies have no Fail state (they don't gate readiness) — `Warning < 70`, `Pass ≥ 70`. No code change needed; A4 was already closed.

8. **Ruling A5 — D8 pillar scoring method. CONFIRMED, no correction needed:** my proposal (weighted intra-pillar per S26) matches the already-shipped implementation in `services/agents/kase_scoring.py`, verified by `tests/test_session26_kase_scoring.py` (`test_aggregate_pillar_scores_matches_worked_example`, which hand-computes the weighted formula and asserts against it). The Chunk 6 Stage-5 simple-average is confirmed illustrative only. No code change needed.

**Note on A3/A4/A5 generally:** all three "open rulings" turned out to already be implemented, tested, and marked `[PROPOSAL, ratified]` in code — the issue log was stale relative to the actual codebase. Going forward, verify shipped code before treating an issue-log item as open.

13. **IN PROGRESS (handoff, not committed/pushed) — Hierarchical demo replay-proof, offline phase.** Working in `/tmp/kt_assist_check` on `demo-mode` @ `1eff195`. New, uncommitted, local-only files (a fresh clone/session will NOT have these until pushed):
    - `services/demo/hierarchical_fixtures.py` — pinned demo IDs (Ruling A: fixed program/package/3-receiver UUIDs, kept out of `config/`).
    - `services/demo/hierarchical_kai_attributes.py` — pilot-profile (`System`/`Known Issue`/`Task`) structured attribute overlay, values derived mechanically from each object's own existing description in `scripts/seed_demo_kai_cache.py` (no invented facts). Also currently contains `SYSTEM_DEPENDS_ON_EDGES` — **superseded, see bug below, do not use as-is.**
    - `services/demo/hierarchical_gap_answers.py` — the 7 evidence-confirmation closure answers (one per Known Issue's withheld `validation_status`/`evidence_refs`), keyed by `(object_id, rule_family)` — confirmed this is the exact same key `services/coverage/consolidation.py` already uses as `KnowledgeGap` identity. Explicit `UnknownGapSignatureError` for unregistered signatures (no silent fabrication). This part is verified working.
    - `services/demo/receiver_strategies.py` — loads the 3 golden `competency_response_strategy` dicts from `datasets/golden/golden_responses.json` (no duplication), with an empty `TUNING_OVERRIDES` layer for Ruling B.
    - `scripts/seed_demo_hierarchical_kai_cache.py` — writes the pilot-suffixed KAI cache (`:pilot-v1` key, confirmed required — see Step-1 finding below) by importing/overlaying onto `scripts/seed_demo_kai_cache.py`'s existing object graph (no duplication). **Currently also injects `SYSTEM_DEPENDS_ON_EDGES` into the shared relationships cache — this needs to be REMOVED per the bug fix below.**
    - `scripts/run_hierarchical_demo_replay_proof.py` — the end-to-end offline proof runner (ingest_hierarchical → validate_hierarchical → closure loop → persist → KAR → generate_assessment → 3 receivers via `build_scenario_responses`/`score_and_persist_readiness`, using a `SimpleNamespace(sufficiency_gate_passed=...)` stand-in for legacy `CoverageResult` and `kar_adapter.adapt_kar_to_gates()` to bridge KAR into the unmodified `score_and_persist_readiness`). **Verified working through the ingest + initial validate steps; fails at the closure step due to the bug below.**

    **Step 1 finding (confirmed, needed the new cache):** `services/agents/kai_extraction._chunk_cache_key()` appends a `:pilot-v{N}` suffix whenever `pilot_object_types` is non-empty, so the original `seed_demo_kai_cache.py` cache entries are NOT hit by `ingest_hierarchical()` — a fresh, separately-keyed pilot cache (with `attributes` populated on System/Known Issue/Task) was required. Relationship-discovery's cache key (`{hash}:relationships`) does NOT fork on pilot mode, so that one file is legitimately shared/extended, not duplicated.

    **Real bug found (not yet fixed, needs a ruling):** `services/agents/kai_relationship_discovery.py::discover_relationships()`'s type-pair acceptance check (`expected_pair = RELATIONSHIP_TYPE_RULES.get(rel.relationship_type)`) only consults the **primary** `RELATIONSHIP_TYPE_RULES` dict, never `RELATIONSHIP_TYPE_RULES_ADDITIONAL` (`schemas/knowledge_graph.py`). So a `System → Dependency` `DEPENDS_ON` proposal — the exact case the "KASE/hierarchical relationship compatibility patch" (main, commit `c1cd851`) added `RELATIONSHIP_TYPE_RULES_ADDITIONAL` to support — is silently rejected as a "type-pair mismatch" during initial KAI relationship discovery, even though `services/coverage/dimensional_scoring.py`'s RC computation and `RELATIONSHIP_TYPE_RULES_ADDITIONAL` itself correctly treat it as valid. Confirmed empirically: seeded 62 relationships (56 original + 6 System→Dependency) into the shared cache; only 56 were persisted after a real `ingest_hierarchical()` run; RC computed as 0.0; 5 RELATIONSHIP_GAP findings fired, blocking the Sufficiency Gate. This is a real, pre-existing gap in the Wave-7 compatibility patch (affects any real Claude-live run too, not just this demo), independent of the demo work.
    - **Decision (workaround, not yet implemented):** do NOT patch `discover_relationships()` in this offline-replay-proof phase (out of scope per this phase's constraints — no lifecycle/logic changes). Instead: stop seeding `SYSTEM_DEPENDS_ON_EDGES` via the initial KAI cache at all; let each System start with an open `RELATIONSHIP_GAP` (rule_family `failure_recovery`), and close it through the **hierarchical closure loop's fixture answer** using `InterpretedRelationshipChange` (`action="create"`, matched by `source_name`/`target_name`) via `services/graph/graph_update.py::apply_interpreted_changes()` — confirmed by reading that function (lines ~125-148) that its relationship-creation path performs **no** type-pair check at all, only a by-name existence check. This proves real closure without touching the buggy code, and is arguably a better demo beat (a genuine KGE-closed relationship gap) than a pre-populated one.
    - **This bug should be reported to Sharath as a separate, real architecture finding** (fix candidate: change `discover_relationships()`'s check to use `schemas.knowledge_graph.all_valid_pairs_for()`/`is_valid_relationship_pair()` instead of the raw `RELATIONSHIP_TYPE_RULES` dict lookup) — independent of whether the demo workaround above is adopted.

    **Diagnostic-only numbers observed** (will change once the relationship-gap workaround is applied — do not treat as final): initial `validate_hierarchical()` on the real 50-object/56-relationship pilot-attributed graph: TC=1.0, AC=1.0, RC=0.0 (the bug above), OS=1.0, EV=0.0 (7 Known Issues' evidence deliberately withheld, as designed), KCS=0.75, KQS=0.7, Sufficiency Gate=False, Quality Gate=False, 5 critical_unresolved_gaps, 2 transition_risks.

    **Remaining steps (not started):** (a) remove `SYSTEM_DEPENDS_ON_EDGES` from the KAI-cache seed path; (b) add a relationship-closure answer set (parallel to the existing evidence-confirmation one) to `hierarchical_gap_answers.py`, built with `InterpretedRelationshipChange`; (c) rerun `run_hierarchical_demo_replay_proof.py` end to end; (d) steps 7-12 of the original task (KAR from closed graph, real `generate_assessment`, 3 receivers' real KASE/KRA outcomes, fixture tuning if needed per Ruling B, tests per the 10-item list, final report). None of steps 7-12 have been executed yet.

    **State/environment note:** repo clone lives at `/tmp/kt_assist_check` (container-local, not guaranteed to persist across sessions). Baseline before this work: `demo-mode` @ `1eff195`, full suite 676 passed/1 skipped, confirmed clean. All new files above are **uncommitted and unpushed** as of this entry — a new chat/session will need either (1) this same container/path if it persists, or (2) the files recreated from scratch using the detail above, or (3) whatever was pushed after this entry was written (check `git log demo-mode` / a `demo-mode-hierarchical-wip` branch for a possible WIP commit made right after this log entry).

9. **A2 — Phase 12 unified invariants suite, closed 2026-07-04.** Added the two missing guards to `tests/invariants/test_architectural_boundaries.py`: (a) Phase 9 number-guard, re-exercised independently against a minimal `ExplanationData` fixture — confirms the narrative layer discards any Claude output with an untraceable number and falls back to deterministic template text; (b) Phase 10 aggregate-don't-re-score guard, re-implemented as a grep over the 3 dashboard service files for the 4 forbidden scoring-formula constants (`OIS_WEIGHTS`, `CRITICALITY_WEIGHTS`, `EVIDENCE_SCORES`, `OBJECT_VALIDATION_SCORES`). Both new tests pass; full suite run at 518 passed, 1 skipped (no regressions). All three guards (Phase 9 number-guard, Phase 10 no-rescore, Phase 11 HTTP-only import) now live in one file per the Phase 12 goal; original per-phase test files kept as-is (belt-and-suspenders, fuller worked examples).

10. **A7 — Windows environment hotfixes, closed 2026-07-04.** Two Windows-only bugs surfaced by local test runs: (a) `OSError` in `services/core/claude_client.py::_cache_path()` — cache keys containing `:` produced invalid Windows filenames; fixed by replacing `:` → `-` before building the path. (b) `UnicodeDecodeError` from `Path.read_text()` defaulting to CP1252 on Windows instead of UTF-8 — fixed by passing `encoding="utf-8"` explicitly at 4 call sites: `test_architectural_boundaries.py::_top_level_imported_modules`, `::_code_level_claude_references` (same bug, not in the original report, fixed for consistency), `::test_dashboard_services_never_reconstruct_scoring_formulas`, and `test_frontend_boundary.py::_imported_top_level_modules`. Full suite re-verified: 518 passed, 1 skipped.

11. **A1 — Phase 13 datasets D1–D3+D8, found already complete 2026-07-04 (not new work).** `datasets/power_bi_dashboard/` already has all of D1 (source doc + core assets), D2 (`intentional_gaps.json` — Known Issue omitted, System/Control/Escalation partial, measured initial coverage 71.05%), D3 (`gap_answers.json` + `coverage_curve.json` — 2 scripted steps reaching 86.84%, deliberately left short of 100% since Escalation/Known Issue stay unresolved). D8 (`golden/golden_responses.json`) has all 3 required outcomes (Ready/Not Ready/Conditionally Ready) measured against the real KASE pipeline. `tests/datasets/` — 8/8 passing. Scope note already in `manifest.json`: Dataset 2 (D4–D6) and Dataset 3 (D7, spec-marked optional) are explicitly deferred/out-of-scope for this build pass, not missing work. Note: this dataset (71.05%→86.84%) is independent of DemoRunner's separate scripted demo scenario (63%→89%, OIS 84) — two different fixtures, not a conflict; `demo_runner.py` does not reference this dataset.

12. **A6 — Repo hygiene, closed 2026-07-04.** Untracked ~1,400 previously-committed runtime artifacts that predated their `.gitignore` rules: all of `assets/<uuid>/` (test-upload files; kept the 4 brand logo files tracked), `data/kt_assist.db.bak`, `data/graphs_smoketest/`, `data/tmp_cache_dbg/`, and 21 `data/.fuse_hidden*` NFS/FUSE artifacts (found during cleanup, same category, already `.gitignore`d but never actually untracked). Removed 3 dead test files: `tests/zz_debug_test.py` and `tests/zz_debug_test2.py` (debug wrappers that just re-invoked `test_demo_runner`'s real test under an alias — no unique coverage) and `tests/test_demo_runner_copy.py` (already an empty placeholder). Removed 3 empty stub files/packages: `agents/__init__.py`, `storage/__init__.py`, `models/__init__placeholder__.py` (confirmed unused — no code imports `agents` or `storage` anywhere). Full suite re-verified after each step: 517 passed, 1 skipped (down from 518 — the 2 removed debug-wrapper tests, expected).
1. **Bug #1 — lifecycle_state desync** (commit `ed3019d`). Executive Dashboard showed "Draft/Not Assessed" after full pipeline because `DemoRunner.run_all()` never advanced program lifecycle. **Fix:** lifecycle now advances **through HTTP endpoints** (not direct service mutation), keeping DemoRunner faithful to real user flow. Files: `services/orchestration/demo_runner.py`, routers, Screen 1. **Why:** HTTP-only guard means the demo must exercise the same path users do.

2. **`domain_breakdown_json` always null in Validation Center.** No code path persisted `CoverageResult` from a live `run_kva()`. **Fix:** created `services/coverage/coverage_persistence.py` as a **leaf shared module** (breaks `graph_update` ↔ `workflow_runner` circular import) and extended the upload endpoint to run `validate()` + `persist_coverage_result()` after `ingest()`. **Why leaf module:** embedding persistence in either orchestration or graph_update reintroduced the cycle.

3. **Stale `graph_version_id` in DemoRunner.** After gap closure advanced graph v1→v2, DemoRunner kept passing v1, corrupting downstream reads. **Fix:** re-read current version post-mutation. Established convention: never cache graph version across mutations. Files: `services/orchestration/demo_runner.py`, `services/graph/graph_update.py`.

4. **`close_gap()` computed but never persisted `KVAResult`.** Every gap response recomputed coverage then discarded it. **Fix:** persist via `coverage_persistence` inside `services/coverage/gap_governance.py`. Discovered (like #2) only by **chaining HTTP calls end-to-end** — codified as a testing principle.

5. **Structural refactor + spec reconciliation.** Flat `services/` → 8 domain subpackages (`a111da7`), dead flat duplicates removed (`469671c`), `config.py` monolith → `config/` package (`bf9e929`); orphaned shadowed `config.py` and duplicate root `frameworks/`/`prompts/` removed 2026-07-04. Spec side (via Cowork): competency catalog reconciled to **12 OIF competencies / 4 pillars** with correct weights + critical flags; KTTL template profiles fixed to exact required/optional object sets per package type. **Why:** Master Spec v2 is the reconciliation authority; code follows spec, never the reverse.

13. **Hierarchical replay-proof resumed, `validate_graph()` patched — closed 2026-07-07.** Same bug class as #8's `discover_relationships()` finding: `services/graph/knowledge_model.py::validate_graph()` also checked only `RELATIONSHIP_TYPE_RULES` (primary), never `RELATIONSHIP_TYPE_RULES_ADDITIONAL` — confirmed deliberate in the original compatibility patch's own docstring, not an oversight. This blocked persisting the closure loop's real System→Dependency edges (created via `InterpretedRelationshipChange`, which has no type-pair check) — `save_graph_version()` rejected them. **Fix:** swapped the raw dict lookup for `is_valid_relationship_pair()` (already existed, used elsewhere). 3 new regression tests (legacy pairs still valid, System→Dependency now accepted, a genuinely invalid pair still rejected). 686 passed/1 skipped (676+3+7 since last log entry — see #12 baseline), zero regressions. `discover_relationships()` itself remains unpatched (separate, still-open finding from #8) — this only fixed the persistence-layer copy of the bug. Real pipeline result after the fix: closure loop resolves 5/6 relationship gaps + 2/7 evidence gaps before hitting sufficiency, both gates flip True (RC 0.0→0.875, EV→0.5), persisted as graph_version=2.

14. **KASE competency-coverage correction (Known Issue→Problem Solving, Escalation→Communication) — closed 2026-07-07.** Investigation (read-only, no code changes) found the real PBI package's real generated scenarios only ever exercise 8 of the OIF's 12 competencies, capping every receiver's max possible OIS at 72.0 (below the 75 Ready threshold) — traced to `config/scoring.py::OBJECT_TYPE_COMPETENCY_MAP` assigning exactly one competency per object type, with 4 competencies (analytical_thinking, problem_solving, communication, knowledge_stewardship) never assigned to any object/relationship type at all, for any package, ever (not hierarchical/demo-specific). Root-cause classification: `problem_solving` and `communication` are **C** (mapping hole — real graph content exists: Known Issue's `trigger`/`impact`/`detection_method`/`resolution_path` attributes map directly to the OIF's PS-01..06 markers; Escalation's contact-handover content supports CM-01..05, and the frozen spec's Chunk 5/SGF table explicitly says Escalation should generate Communication Scenarios). `knowledge_stewardship` is **B** (graph has Finding/KnowledgeGap content that could support it, but `scenario_generation.py` never reads Wave 1-6 structures at all). `analytical_thinking` is **E** (no natural anchor, would need a new cross-object scenario mechanism). **Approved fix (Ruling, this session):** additive-only — `config.OBJECT_TYPE_COMPETENCY_MAP_ADDITIONAL` (`{"Known Issue": ["problem_solving"], "Escalation": ["communication"]}`) + new `config.competencies_for_object_type()` helper, combined at every read site (`scenario_generation.py`'s 2 call sites, `scenario_validation.py`'s Layer-3 independent-grounding check — the latter would otherwise reject the new mappings as "ungrounded", breaking the generator/validator circularity-break design). Canonical `OBJECT_TYPE_COMPETENCY_MAP` itself untouched. Knowledge Stewardship/Analytical Thinking explicitly NOT added (would need new scenario-generation mechanisms — future product work, not this patch). 13 new tests (`tests/test_competency_coverage_correction.py`) + 4 existing test assertions updated (`test_session21_scenario_generation.py`, for Known Issue/Escalation's now-correct 2-competency mapping). 699 passed/1 skipped (686+13), zero regressions. Real pipeline result: 10/12 competencies now exercised; Priya (all-Demonstrated) → OIS=85.0, **Ready** (previously capped at 72.0, Conditionally Ready) — confirms the ceiling was real and is now fixed by legitimate coverage, not by forcing an outcome.

15. **Open finding — "Conditionally Ready" may be structurally unreachable on this scenario set, pending ruling.** After #14's fix, re-ran the 3 golden receiver strategies: Priya→Ready (85.0, matches golden), Receiver C→Not Ready (71.7, matches golden), but **Receiver B→Ready (75.27)**, not the golden-expected Conditionally Ready. Empirical per-competency analysis (via ad-hoc harness, not committed code): every one of the 6 Critical competencies is effectively **pinned** for any gate-passing strategy on this real scenario set — `exception_handling`/`dependency_awareness`/`compliance_control_awareness` are undiluted (their own status dominates 100% of their scenarios, both object- and relationship-sourced) so their only gate-passing (≥70) value is their max (~100); `escalation_awareness` is diluted 50/50 with `communication` (via `ESCALATES_TO`'s alphabetical competency-list ordering putting `communication` first) and its only gate-passing blend is 75 (its current value); `process_execution` is diluted by `tool_proficiency`/`risk_awareness` (already at their floor, `Missing`) down to 85.5, its lowest safe value; `decision_making` is unresponsive to its own strategy key entirely (the critical-coverage round-robin patch, `scenario_weighting.py::_ensure_critical_coverage`, appends it last to some other scenario's competency list, so `build_scenario_responses`'s first-match-wins logic never actually reads `decision_making`'s own strategy value — same competency-sharing mechanism, a separate minor quirk worth knowing about). Sum of all pinned-minimum values = OIS 75.27, which is **entirely inside Ready territory (≥75)** — meaning gate-passing strategies cannot produce OIS in [72,75), and gate-failing strategies are unconditionally "Not Ready" regardless of OIS (checked before the boundary-zone comparison, per `threshold_model.py`). Since scoring depends only on the response-strategy dict and the fixed graph/scenario set (not on which participant_id it's attached to), this floor is receiver-agnostic — reassigning roles wouldn't help either. **Not yet resolved — needs a ruling**: (a) accept 2 distinct outcomes (Ready/Not Ready) as correct given this real package's evidence granularity, or (b) some other approach Sharath prefers. No code changed for this finding; it's reported from an ad-hoc, uncommitted diagnostic script only.

    **Update to #15 (2026-07-07, read-only investigation, no code changed):** Root cause is now fully traced, not just empirically observed. `resolve_readiness()` (`services/readiness/threshold_model.py`) is mathematically correct and has a genuine non-empty Conditionally Ready interval ([72,75) for Primary tier) — this is NOT a KRA limitation. The real package's generated scenario set is also not fundamentally incapable: several critical competencies have many contributing scenario instances (`process_execution`=83, `tool_proficiency`=48, `dependency_awareness`=19, `exception_handling`=14, `problem_solving`=14, `escalation_awareness`=8, `compliance_control_awareness`=7, `decision_making`=1 only), so competency scores are means across real, multiple instances and are NOT inherently locked to {0,50,100}. The actual blocker is the **fixture abstraction**: `receiver_strategies.py`'s `competency_response_strategy` dict (consumed by `workflow_runner.py::build_scenario_responses`) sets one uniform Demonstrated/Partial/Missing label per competency NAME, applied to every scenario where that name happens to be the first-alphabetical (relationship scenarios) or first-canonical (object scenarios) match — it cannot vary response quality across the many individual scenario INSTANCES within one competency. Confirmed via golden dataset comparison: `datasets/golden/golden_responses.json`'s `conditionally_ready_boundary_zone` entry reaches OIS=74.5 cleanly by lowering ONLY non-critical competencies that are NOT scenario-entangled with any critical one in that synthetic, one-scenario-per-competency dataset — a structural difference from the real package, where every present non-critical competency (`tool_proficiency`, `risk_awareness`, `communication`) turns out to be scenario-entangled with a critical partner, and all of that entanglement dilution is already fully spent in the current 75.27 floor. **Root cause classification: A (coarse Demonstrated/Partial/Missing evidence states) + C (scenario-to-competency overlap coupling entangles non-critical dilution with critical competencies) + G (the fixture's competency-level abstraction, not KASE itself, is what's coarse) — not B (most criticals have ample scenario counts), not D/E (aggregation math is correct and is what would enable finer control if exposed), not F (boundary-zone logic is fine), not H (no defect).** Smallest genuine fix identified (not implemented — needs a ruling): a new, fixture-only, per-scenario-instance (or per-object-id) response table in `services/demo/receiver_strategies.py`, consulted by a new small fixture-side function analogous to `build_scenario_responses` but keyed by scenario `source_id` instead of competency name — zero KRA/KASE/scoring changes, since it only changes which response_text gets attached to which (Scenario, ScenarioResponse) pair before the existing, unmodified scoring pipeline runs.

16. **Scenario-level replay fixture — closed 2026-07-07. Resolves #15.** Investigation (previous entry) confirmed the 75.27 floor was a **fixture abstraction limit, not a KASE/KRA limit**: `resolve_readiness()`'s Conditionally Ready interval ([72,75) for Primary tier) is real and mathematically reachable, and most Critical competencies have ample real scenario instances (process_execution=83, tool_proficiency=48, dependency_awareness=19, exception_handling=14, problem_solving=14, escalation_awareness=8, compliance_control_awareness=7) — the blocker was that `receiver_strategies.py`'s `competency_response_strategy` dict can only apply one uniform status per competency NAME, never per scenario instance. **Fix:** new `services/demo/receiver_strategies.py::build_receiver_scenario_responses()` — a demo-only sibling of `WorkflowRunner.build_scenario_responses` (zero changes to KRA/KASE/threshold_model.py/evidence detection/competency or pillar aggregation/scenario generation/competency mappings). Resolution order: `SCENARIO_LEVEL_OVERRIDES[participant_id][(source_kind, source_id)]` → competency-level strategy → default. Key verified 100% unique and stable across the real 99-scenario set (derived from the underlying KnowledgeObject/Relationship id, fixed by the content-hash-cached KAI extraction — unlike `Scenario.id`/`AssessmentPackage.id`, which are fresh UUIDs every `generate_assessment()` call). Unmatched override keys raise `UnknownScenarioOverrideKeyError` (never silently dropped). Receiver B's pattern (empirically tuned, grounded in real transcript content): Partial on 2 Known Issues the transcript describes as genuinely undocumented (`ki-no-sop`, `ki-returns-workspace-uncertain`) + Partial on 1 fragile Dependency (`dep-pbi-version`, the October 2024+ version requirement) — everything else stays at the existing golden `conditionally_ready_boundary_zone` baseline. Result: OIS=73.535, gate=True, boundary_zone_applied=True, **Conditionally Ready** — matches golden expectation exactly. 5 new tests (override precedence, fallback-to-competency-baseline, unknown-key error, no-override-configured-participant unaffected) + updated the two full-lifecycle tests to use the new builder for all 3 receivers, asserting all 3 now hit their golden decisions with `len(set(results.values())) == 3`. 713 passed/1 skipped (708+5), zero regressions. **Final replay-proof result, all real (no hardcoded scores/decisions)**: Priya OIS=85.0 Ready/Silver; Receiver B OIS=73.535 Conditionally Ready (boundary zone); Receiver C OIS=71.7 Not Ready (critical gate fails on exception_handling=50). Deterministic: reran the full script twice, byte-identical numbers both times.

17. **Demo orchestration layer + deterministic reset/checkpoint model — closed 2026-07-07.** New thin orchestration layer coordinating the already-validated hierarchical lifecycle (replaces ad-hoc sequencing in scripts/run_hierarchical_demo_replay_proof.py, which is now a thin CLI wrapper around it). Owns sequence/checkpoint state only -- zero duplication of KAI extraction, ValidationPlan/Finding/consolidation/prioritization, enrichment interpretation, graph update/revalidation, dimensional scoring, gate evaluation, Transition Risk derivation, KAR construction, KASE scoring, or KRA decision logic; every call delegates to the existing real services.
    - **New model**: `models/demo_journey.py::DemoJourneyState` (table `demo_journey_states`) — one row per package_id, stages `START -> INGESTED -> VALIDATED -> ENRICHING -> ASSURANCE_COMPLETE -> ASSESSMENT_COMPLETE`. Orchestration metadata only (stage, profile_id, graph_version_number, closure_rounds_completed) — never a second source of truth for Findings/Gaps/scores/gates/KAR/KASE/KRA (those are always re-read from the real services, e.g. `get_demo_summary()` calls `validate_hierarchical(persist=False)` fresh rather than caching gate state).
    - **New orchestrator**: `services/demo/hierarchical_demo_orchestrator.py::HierarchicalDemoOrchestrator` — `get_demo_state()`, `reset_demo()`, `ingest_demo()`, `validate_demo()`, `advance_enrichment(max_rounds=1)`, `complete_assurance()`, `assess_receiver(participant_id)`, `get_demo_summary()`. Identity get-or-create helpers moved here from the script (same fixed demo ids, unchanged behavior).
    - **Checkpoint model**: each stage transition is persisted only *after* the real underlying call succeeds (and, where applicable, its own real persistence function ran — `save_graph_version`, `validate_hierarchical(persist=True)`, `score_and_persist_readiness`). A raised exception leaves the journey stage/graph version untouched, so resuming re-attempts safely (verified: injecting a raising interpretation function mid-closure leaves the previous checkpoint exactly as it was; a normal resume afterward completes real closure).
    - **Idempotency**: every write operation checks persisted state first — `ingest_demo()` skips re-ingestion if a graph version already exists; `assess_receiver()` skips recomputation if a `ReceiverReadiness` row already exists for that (package_id, participant_id); all 3 receivers share ONE `AssessmentPackage` (generated once per graph version via `_get_or_create_assessment_package`, not once per receiver — avoids the "3 separate 99-scenario sets" waste a naive per-call `generate_assessment()` would create). Stage advancement is strictly monotonic (`advance_enrichment` only moves VALIDATED->ENRICHING, never regresses a later stage back).
    - **`advance_enrichment(max_rounds=N)`** supports both one-round-at-a-time (N=1, for a UI to show live progress) and full-closure-in-one-call (N=50, matching the original script) via the same parameter — verified both work identically against the real `run_hierarchical_closure_loop`.
    - **Reset**: `reset_demo()` deletes every row transitively tied to the fixed demo package/participant ids (graph versions, coverage/KAR rows, assessment packages/scenarios/responses, evidence/competency/pillar/OIS/readiness rows, the journey row) in FK-safe order (same per-table-flush pattern as `scripts/reset_demo.py`, needed because SQLite FK enforcement — `PRAGMA foreign_keys=ON` — rejects a single end-of-function flush when cross-table deletes aren't individually flushed in dependency order first), then re-establishes program/package/3 participants at a fresh START. Idempotent (verified: reset-twice gives identical state). Never touches any other program/package/participant. New `scripts/reset_hierarchical_demo.py` CLI wrapper (separate from the legacy `scripts/reset_demo.py`, which cleans up the *v1* `python cli.py demo` runbook instead — different pipeline, different fixed ids, correctly left untouched).
    - **API**: new `services/routers/demo_hierarchical.py`, prefix `/api/demo/hierarchical`, mounted in `app.py` alongside every existing router (untouched). `GET /state`, `POST /reset`, `POST /ingest`, `POST /validate`, `POST /enrichment/advance`, `POST /assurance/complete`, `POST /receivers/{participant_id}/assess`, `GET /summary`. Verified end-to-end via FastAPI TestClient — full lifecycle produces the same 3 real outcomes as the CLI script.
    - **Result, all real, deterministic, verified via `tests/test_hierarchical_demo_orchestrator.py` (25 tests)**: Priya OIS=85.0 Ready/Silver; Receiver B OIS=73.535ish Conditionally Ready (boundary zone); Receiver C OIS=71.7 Not Ready. No Anthropic SDK call anywhere in the orchestrated lifecycle (`client._sdk_client is None` after a full run).
    - Full suite: 738 passed, 1 skipped, zero regressions (713 + 25 new). Legacy v1 demo/workflow (`tests/test_demo_runner.py`, `tests/test_reset_demo.py`, `tests/level3/test_full_workflow.py`) explicitly re-confirmed green and untouched.
    - No UI added (out of scope this phase, per spec). No new infrastructure (same SQLite DB, same session patterns).

18. **UI Phase 1 — Executive Command Center + Guided Demo Shell — closed 2026-07-07.** New presentation layer, additive only: the 10 existing generic screens (streamlit_app.py's Overview/Programs/Knowledge Packages/Receivers groups) are completely unmodified. A new "Executive Command Center" nav group (2 pages) is now the app's default landing page.
    - **New**: `frontend/guided_demo/portfolio_fixture.py` — 15 static, deterministic synthetic transition cases (12–18 target range) across 7 business functions, plus exactly one real case (`PBI_CASE_ID`). Pure aggregation functions only (KPIs, readiness distribution, business-unit rollup, executive-attention selection) — every number the dashboard shows traces to this single list, no parallel totals. Lives under `frontend/` (not `services/demo/` as the task's suggested structure implied) because `tests/test_frontend_boundary.py` mechanically forbids any `frontend/` file from importing `services/` — this is pure presentation data with no backend meaning, so it belongs on the frontend side of that boundary. Noted as a deliberate, reasoned deviation from the task's *suggested* (not mandatory) file layout.
    - **New**: `frontend/guided_demo/executive_dashboard.py` (KPI strip, readiness/assurance/exposure charts via native `st.bar_chart`, Executive Attention panel, Transition Portfolio table with a case selector) and `frontend/guided_demo/guided_shell.py` (real PBI case: 7-conceptual-stage progress tracker mapped from the real 6 `DemoJourneyState` stages + receivers-assessed count, never inventing a new backend stage; synthetic cases: lightweight static detail view whose renderer takes no `ApiClient` parameter at all — structurally incapable of calling the demo API, satisfying "do not run the real lifecycle for synthetic cases").
    - **PBI case identity**: never hardcoded in the frontend (would violate the same `services/` import boundary) — the real `package_id`/`program_id`/receiver names/outcomes are always discovered live via `ApiClient.get_demo_summary()` and merged onto the one static placeholder row (`apply_real_pbi_state`). Once all 3 real receivers are assessed, the row's readiness status becomes `"Mixed Readiness"` rather than a misleading single-decision label — and the readiness-distribution KPI counts each of the case's 3 real receivers into their own actual bucket (Ready/Conditionally Ready/Not Ready), not into one composite bucket, so portfolio totals still reconcile exactly.
    - **Guided shell CTAs are real, single-click actions** against the already-complete, already-tested demo orchestration API (`ingest_demo_hierarchical` / `validate_demo_hierarchical` / `advance_demo_enrichment(50)`+`complete_demo_assurance` / `assess_demo_receiver` per pinned receiver) — deliberately not a per-gap Q&A UI or per-scenario response builder (both explicitly out of scope for this phase); each button is one deterministic call to a capability that already exists and is already tested, then a rerun.
    - **`frontend/api_client.py`**: 8 new thin wrapper methods (`get_demo_state`, `get_demo_summary`, `reset_demo_hierarchical`, `ingest_demo_hierarchical`, `validate_demo_hierarchical`, `advance_demo_enrichment`, `complete_demo_assurance`, `assess_demo_receiver`) — plain-dict returns (no new Pydantic schema module) since these are demo-only, self-describing responses, consistent with the backend router's own `list[dict]` precedent for non-core hierarchical endpoints.
    - **`streamlit_app.py`**: added the 2 new pages + a `st.session_state["_nav_pages"]` registry so guided-demo screens can `st.switch_page()` between each other (needed because this repo's nav is entirely function-based via `st.Page(callable, ...)`, so there's no file path to pass to `switch_page` instead).
    - 26 new tests (`tests/test_guided_demo_portfolio.py`): fixture determinism/case-count, KPI/readiness/business-unit reconciliation, executive-attention sourcing, PBI identity mapping via a real `TestClient`-backed `ApiClient`, synthetic-case renderer's structural inability to call the API, resume-CTA-per-stage and conceptual-progress-index mapping, api_client wrapper round-trips, no-Anthropic-call-with-no-API-key, and existing screens still registered. Verified end-to-end with `streamlit.testing.v1.AppTest` (no exceptions, Executive Command Center is the default page).
    - Full suite: 764 passed, 1 skipped, zero regressions (738 + 26). `tests/test_frontend_boundary.py` re-confirmed green with the new files.
    - No backend lifecycle/scoring/threshold logic touched. No detailed KAI/KVA/KGE/KAR/KASE/KRA screens built (explicitly deferred to UI Phase 2/3, per scope).

19. **UI Phase 2 — Knowledge Lifecycle Experience — closed 2026-07-07.** Detailed guided scenes for the 5 real backend stages (Knowledge Intake/Discovery/Assurance/Gap Closure/Assurance Result), integrated into the Guided Demo Case Shell's existing 7-tab progress tracker. Receiver Assessment/Readiness Decision (tabs 6/7) remain visible placeholders only, per UI Phase 3 scope.
    - **Inspection findings**: `/api/demo/hierarchical/summary` and `/state` already existed but didn't expose discovery/gap/closure-round detail. `services/routers/hierarchical.py`'s existing endpoints recompute Findings/Knowledge Gaps live (never persisted) — confirmed the same real-recompute pattern was safe to reuse for demo presentation needs. `HierarchicalClosureResult`/`ClosureRoundRecord` (services/coverage/hierarchical_closure.py) hold real per-round question/targeted-object/resolved-finding-signature detail but get discarded once `advance_enrichment()` returns — nothing persisted them past one call. The real "before enrichment" snapshot needs no new storage at all: graph version 1 always exists post-ingest, so `validate_hierarchical(version=1, persist=False)` recomputes it fresh and exactly, forever.
    - **One additive presentation contract added** (per the phase's explicit ruling allowing this): `models/demo_journey.py::DemoJourneyState.closure_round_history_json` — a JSON list of qualitative round interactions (object/rule_family/criticality/risk_level/question/**deterministic SME response text**/resolved-finding-count/graph-version-before-after). This is the *one* piece of real closure output that is not otherwise reconstructable (unlike KCS/KQS/dimensions, which `get_closure_history()` always recomputes fresh per round via `validate_hierarchical(version=N)` against the real bracketing graph versions — never stored). Captured via a thin wrapper around `get_interpretation_for_gap` inside `advance_enrichment()` (records `raw_text` as a side effect) plus an independent, read-only pre-call gap snapshot (same real `build_validation_plan`/`detect_all_findings`/`consolidate_findings`/`rank_gaps` functions `services/routers/hierarchical.py` already calls, for criticality/risk_level/object-name lookup) — zero changes to `services/coverage/hierarchical_closure.py` or any other core hierarchical service. Needed a small ALTER-TABLE migration (`scripts/migrate_demo_journey_round_history.py`, same pattern as the Wave 7 migration) since the table already existed in the committed demo DB.
    - **New orchestrator methods** (`services/demo/hierarchical_demo_orchestrator.py`): `get_discovery_summary()` (real object/relationship counts, object-type distribution, attribute-state distribution via the real `KnowledgeElementState` enum, grounding examples), `get_knowledge_gaps_detail()` (real ranked Findings/Knowledge Gaps with human-readable object names), `get_pre_enrichment_kar()` / `get_assurance_snapshot()` (before/current KAR, both always recomputed fresh, never stored), `get_closure_history()`, `get_traceability_example()` (built from the real closure history + graph, no new traceability subsystem — `services/explanation/traceability_service.py` is the KASE/OIS-side chain, a different concern reused in UI Phase 3, not this one).
    - **New router endpoints** (`services/routers/demo_hierarchical.py`): `GET /discovery-summary`, `GET /knowledge-gaps`, `GET /assurance-snapshot`, `GET /closure-history`, `GET /traceability-example`. **8 new `frontend/api_client.py` wrappers** to match.
    - **New frontend files**: `frontend/guided_demo/presentation_labels.py` (rule_family/attribute-state → human-readable labels, presentation-only, never touches backend taxonomy), `frontend/guided_demo/lifecycle_scenes.py` (5 scene renderers). `frontend/guided_demo/guided_shell.py` now renders these via `st.tabs()` (7 tabs matching the conceptual stage list; tabs 6/7 are Phase-3 placeholders) instead of one generic "Current State" block.
    - **Gap Closure scene**: shows before→current (Knowledge Gaps/KCS/KQS/gates), an "Apply SME Response & Revalidate" button (`advance_demo_enrichment(max_rounds=1)`) for one-round-at-a-time pacing plus a "Run Remaining Closure" bulk button, and up to 6 representative interactions (one per distinct real rule_family theme present in the real history, newest first) — explicitly captioned with how many real rounds ran in total vs. how many are highlighted, never presenting the highlighted subset as the complete history.
    - All mutating actions are explicit button presses only (verified: `test_passive_reads_do_not_mutate_state`); every GET is read-only/recompute-only.
    - 17 new tests (`tests/test_guided_demo_lifecycle_scenes.py`): discovery/attribute-state/gap-count reconciliation, real dimension/gate values (not recomputed in frontend), label mapping, real advance-endpoint call + metric movement, no-mutation-on-passive-read, before/after closure-history reconciliation against real graph versions, traceability None-before/real-after, frontend boundary AST check on the 3 new files, no-Anthropic-call. Verified end-to-end with `streamlit.testing.v1.AppTest` at START, at ASSURANCE_COMPLETE (all 5 real scenes), and for a synthetic case (structurally cannot reach the demo API).
    - Full suite: 781 passed, 1 skipped, zero regressions (764 + 17). UI Phase 1 tests and the frontend boundary test re-confirmed green.
    - No scoring/finding-detection/consolidation/prioritization/enrichment/closure/graph-update/KAR/Transition-Risk/KASE/KRA logic touched.

20. **UI Phase 3 — Receiver Assessment / Competency Evidence / Readiness Decision / Executive Recommendation — closed 2026-07-07.** Completes the Guided Demo Case Shell's tabs 6/7 (previously Phase-3 placeholders) with 5 scenes + a cross-receiver comparison, all read from the real KASE/KRA pipeline.
    - **Real bug found and fixed during inspection**: `HierarchicalDemoOrchestrator._rollup_from_existing_readiness()` (the idempotent re-fetch path for an already-assessed receiver) was hardcoding `effective_threshold=0` and inferring `boundary_zone_applied` from the decision string, instead of a real value — harmless for Phase 1/2 (nothing read those fields yet) but would have shown a fake "0" threshold in Phase 3's Readiness Decision scene. Fixed by recomputing the real `ThresholdResolution` via `services.readiness.threshold_model.resolve_readiness(ois_score, role_tier, critical_gate_passed=all_gates_passed)` — a pure function of already-persisted values, so this is re-deriving, not duplicating, scoring logic. Regression-tested explicitly (`test_threshold_and_boundary_zone_are_real_not_placeholder`, `test_receiver_b_boundary_zone_survives_idempotent_refetch`).
    - **One additive endpoint**: `GET /api/demo/hierarchical/receivers/{participant_id}/assessment-detail` (`HierarchicalDemoOrchestrator.get_receiver_assessment_detail()`). Returns real scenario_count/categories/competencies_exercised (from the shared `AssessmentPackage`), and — once assessed — real `competency_scores`/`pillar_scores`/`ois_score`/`critical_competency_gate_passed`/`final_decision`/`certification_level`/`effective_threshold`/`boundary_zone_applied` (all read back exactly as `score_and_persist_readiness` computed them, via the now-fixed `_rollup_from_existing_readiness`), plus a small, fully deterministic representative-interaction selection (up to 3 non-"Demonstrated" real `EvidenceMarkerResult`-backed interactions first — this is exactly where Receiver B's real scenario-level evidence variation surfaces, with zero need to read the fixture's `SCENARIO_LEVEL_OVERRIDES` from the frontend or orchestrator-read-path — then enough "Demonstrated" ones to reach 6, preferring distinct competencies). No scoring/aggregation reimplemented; the only new computation is which few real interactions to highlight.
    - **New frontend file**: `frontend/guided_demo/receiver_scenes.py` — `render_receiver_assessment_setup` (assurance recap + 3 receiver cards + select/run), `render_assessment_experience` (scenario/category/competency counts + representative interactions, explicitly captioned when a receiver's real evidence varies), `render_competency_profile` (OIS hero, critical-gate badge, pillar bar chart, full 12-competency evidence matrix using `config.COMPETENCY_CATALOG` — imported directly in the frontend, same established precedent as `frontend/theme.py`'s `config.COLORS` import, confirmed not to violate the mechanically-enforced boundary since `config` isn't in `FORBIDDEN_TOP_LEVEL_MODULES` — with honest "Not Exercised" labeling for the 2 competencies this package's real scenarios never cover, never shown as failed; deterministic Strengths/Development-Areas via simple score thresholds, no new scoring engine), `render_readiness_decision` (real gate/threshold/boundary-zone values, decision-specific real-value explanation for each of the 3 outcomes), `render_executive_recommendation` (deterministic recommendation copy selected by real decision + populated with real competency names, never a hardcoded per-receiver narrative), `render_cross_receiver_comparison` (table over the 3 real persisted results — never computes a composite case-level status, matching the portfolio dashboard's existing "readiness is receiver-specific" principle from issue_log #18).
    - `presentation_labels.py` extended with `pillar_label`/`competency_label`/`evidence_quality_label` (simple deterministic score-threshold classification: ≥85 Demonstrated, ≥50 Partial Evidence, else Insufficient Evidence, None → Not Exercised) — presentation-only, no new backend taxonomy.
    - `guided_shell.py`'s tabs 6/7 now hold 3 sub-tabs each (Setup/Assessment Experience/Competency Profile; Readiness Decision/Executive Recommendation/Cross-Receiver Comparison) — fits into the existing 7-tab structure rather than a parallel nav system, per instruction. Selected receiver held in `st.session_state["guided_demo_selected_participant_id"]` (presentation-only selection state, not authoritative — every scene re-fetches real detail by participant_id on every render).
    - 18 new tests (`tests/test_guided_demo_receiver_scenes.py`): setup/assurance reconciliation, representative-interaction determinism, Receiver B's real evidence variation, competency/pillar score reality, honest not-exercised labeling, OIS/gate/decision/certification matching persisted backend exactly, all 3 real outcomes (Priya/B/C), real threshold/boundary-zone values surviving idempotent re-fetch (regression test for the bug above), cross-receiver 3-distinct-outcomes, assessment idempotency (no duplicate `ReceiverReadiness` rows), resume-after-reload (fresh `ApiClient`/`TestClient` instance against the same persisted DB), frontend boundary AST check, no-Anthropic-call.
    - Full suite: 799 passed, 1 skipped, zero regressions (781 + 18).
    - No KASE/KAR/KRA/scoring/threshold/certification/closure logic changed — only a real-value bug fix in the demo orchestrator's own idempotent-read path, plus one new read-only presentation endpoint.

21. **Graph storage path portability fix — closed 2026-07-07.** Root cause of HTTP 500s reported when running the app from a different machine/OS (specifically Windows, `C:\Users\...`): `KnowledgeGraphVersion.storage_path` was persisted as a fully-resolved, machine-specific ABSOLUTE path at write time (`services/graph/graph_storage.py::save_graph_version()` did `storage_path=str(storage_path)` where `storage_path` was built from `config.GRAPH_STORAGE_DIR`, itself resolved fresh in whatever environment wrote it). `load_graph_version()` then did `Path(row.storage_path).read_text()` directly — meaningless on a different machine/OS. This is the single root cause behind all 5 reported failing flows (`/summary`, `/graph`, `/graph/html`, package upload, `/explanations/{id}`) — none of those call sites were individually at fault; they all go through this one module.
    - **Confirmed via inspection of the actual local data state** (this sandbox's own committed DB, which itself had accumulated legacy paths from at least 2 prior sandbox environments — `/tmp/kt_assist_check/...` and `/home/claude/kt_assist_new/...` — a live example of exactly the reported bug): the demo package's 8 graph versions DID have their JSON files present locally (`data/graphs/{demo_package_id}/v1..v8.json` all existed) — only the DB row's stored path was wrong. One unrelated, orphaned package (`87434da6...`, 2 versions, not the demo package) had genuinely NO local graph files at all — confirming both the "recoverable via path repair" and "genuinely missing, needs reseed" cases the task asked to distinguish both occur in practice.
    - **Fix**: `storage_path` is now written as a portable, repository-relative path (`data/graphs/{package_id}/v{n}.json`, resolved against `config.BASE_DIR`). Both `save_graph_version()` and `load_graph_version()` resolve `storage_path` through one new centralized policy, `_resolve_graph_path()`, tried in order: (1) portable relative path against `config.BASE_DIR`; (2) the stored value as-is, if it happens to still be a valid absolute path in this environment; (3) deterministic relocation — every graph file's canonical name is always `v{version_number}.json` under the CURRENT `config.GRAPH_STORAGE_DIR/{package_id}/`, regardless of what machine/OS wrote the stored path, so re-deriving that exact location from `(package_id, version_number)` recovers legacy Linux/Windows/temp-directory paths without parsing the stored string at all. Raises a new `GraphArtifactNotFoundError` (never `FileNotFoundError`, never a silent fallback to another version or an empty/fabricated payload) if none of the three resolve. No other module (explanation_data_layer.py, graph router, demo_hierarchical.py, workflow_runner.py) needed or received any path-repair logic of its own — they all consume graphs through `load_graph_version()`/`save_graph_version()` and inherited the fix automatically, per the task's explicit requirement.
    - **`save_graph_version()`'s own internal read of the previous version's `graph_id`** (needed for graph_id continuity across enrichment increments) was the other real failure point ("upload path... attempts to read the previous graph version using the stale absolute storage_path") — now also goes through `_resolve_graph_path()`.
    - **New migration/repair utility**: `scripts/migrate_graph_storage_paths.py` (`--dry-run` supported). Scans every `KnowledgeGraphVersion` row; for each, verifies resolvability via the same real resolver (no separate logic), and if the row's stored path differs from the portable form but DOES resolve, rewrites it to the portable form ("repaired"); if it doesn't resolve at all, reports "unresolved" and leaves the row **completely untouched** (never points a row at a wrong file). Idempotent (a second run reports everything as "already_valid"). Run once against this sandbox's own demo DB: 8 rows repaired (demo package), 2 rows correctly left unresolved (the unrelated orphaned package with no recoverable local file).
    - **Test regression found and fixed**: `tests/test_session8_graph_storage.py`'s own round-trip test opened `version_row.storage_path` directly with a raw `open()`, assuming an absolute-path contract — updated to resolve through the same `_resolve_graph_path()` real consumers use, rather than assuming a raw path format (this is the one existing test the task's own inspection list flagged as relevant: "tests that create graph versions in temporary directories").
    - **14 new tests** (`tests/test_graph_storage_path_portability.py`): same-environment round-trip with portable-path assertion, storage-root relocation (simulated directory move), legacy Linux absolute path resolved deterministically, stale temp path with an equivalent local artifact, stale path with no artifact raising `GraphArtifactNotFoundError` (never silent/empty), exact-version-requested never falls back to a different version even when broken, `save_graph_version()` after a previous row had a legacy path (graph_id continuity preserved), migration utility repairs + reports unresolved + is idempotent + never mutates unresolved rows, and 3 full HTTP round-trip tests (`/summary`, `/graph` + `/graph/html`, `/explanations/{id}`) proving each of the 5 originally-reported failing flows now survives a simulated relocation.
    - Full suite: 813 passed, 1 skipped, zero regressions (799 + 14).
    - **No scoring/gate/threshold/ontology/lifecycle/orchestrator logic changed** — this was purely a storage-boundary portability fix, confined to `services/graph/graph_storage.py` + one new migration script + one existing test's assumption update.
    - Existing local application data (this sandbox's committed demo DB) was preserved — no reseed was necessary; the migration script repaired it in place.

22. **Demo artifact distribution/bootstrap reproducibility fix — closed 2026-07-07.** Follow-up to #21: runtime acceptance testing on an actual fresh Windows checkout hit a 404 (`graph_artifact_not_found`, package `...d2` version 8) that the #21 resolver correctly raised rather than suppressing — this investigation confirms *why*, and fixes the actual distribution gap (not the resolver, which was already correct).
    - **Root cause, confirmed precisely**: `git ls-files data/graphs/` → **zero** tracked files. `.gitignore` line 8 (`data/graphs/`) excludes the entire directory by design — same pattern as `data/cache/kai/` (the hierarchical KAI cache), where only the *generating source code* is committed, never the generated artifact. The committed `data/kt_assist.db` (force-tracked, overriding `data/*.db` in `.gitignore`) is the odd one out: its `KnowledgeGraphVersion` rows referenced graph JSON files that only ever existed on the machine that generated them. My earlier #21 conclusion — "8 rows repaired, no reseed needed" — was **environment-local and not valid for a fresh checkout**: that sandbox happened to already have locally bootstrapped the same demo package, so the repaired portable paths pointed at real local files by coincidence. A genuinely fresh clone (or a different sandbox/machine) gets the DB row but none of the `data/graphs/*.json` files, since those were never (and architecturally should never be) committed.
    - **Architecture determination**: bootstrap/generate (Option B), not committed fixtures (Option A) — confirmed as the existing, correct, already-consistent design for `data/graphs/`. `scripts/reset_hierarchical_demo.py` + `scripts/run_hierarchical_demo_replay_proof.py` (or the equivalent orchestrator call sequence) already fully and deterministically regenerate the KAI cache (from committed Python source constants, not files) and every graph JSON file from scratch — this required zero code changes, since it already works (exercised dozens of times across this session).
    - **The actual fix, since the resolver itself needed no redesign**:
      1. New read-only (by default) consistency validator, `scripts/validate_graph_artifacts.py` (`--repair`, `--package-id` supported) — classifies every `KnowledgeGraphVersion` row as valid/relocatable/missing, plus reports orphaned on-disk files with no DB row. "Missing" rows are never touched (only relocatable ones are, and only via the existing, already-tested `scripts/migrate_graph_storage_paths.migrate()` — no duplicate repair logic).
      2. **The committed demo DB itself was reset to `START`** (`scripts/reset_hierarchical_demo.py`) before this commit — zero `KnowledgeGraphVersion` rows for the demo package now ship in the repository. This is the actual, root-level fix for fresh-checkout reproducibility: a fresh clone's committed database can no longer reference a graph artifact that doesn't exist, because it references none at all. The demo naturally begins at Knowledge Intake; clicking through the guided shell (or running the bootstrap script) regenerates the real graph, cache, and assessment state from scratch on whatever machine is running it. This is a demo-data decision, not a code change — no orchestrator/resolver/lifecycle logic changed for this issue.
    - **8 new tests** (`tests/test_demo_bootstrap_reproducibility.py`): an orphaned `KnowledgeGraphVersion` row (simulating a copied database with no local artifact) raises the real `GraphArtifactNotFoundError` (not fabricated data, not a fallback); the demo summary endpoint surfaces that same real error; the validator classifies such a row as "missing" (never "relocatable"); a full bootstrap (reset → ingest → validate → closure → assess) from that exact orphaned state fully recovers a working, self-consistent demo (Priya → Ready/85.0, matching the always-real pipeline); the bootstrap writes only portable relative paths (no leading `/`, no Windows drive letter); exact graph-version preservation survives the bootstrap-from-orphaned-state path; and both `/summary` and `/explanations/{id}` work correctly on a freshly-bootstrapped demo.
    - Full suite: 821 passed, 1 skipped, zero regressions (813 + 8).
    - **No resolver, orchestrator, scoring, or lifecycle logic changed** in this entry — #21's `_resolve_graph_path()` was already correct (proven by this real-world 404 being exactly the intended, non-silent behavior for a genuinely-missing artifact). This entry closes the distribution/bootstrap gap around it: a validator to detect the mismatch proactively, and a policy decision (never ship a committed DB with dangling graph-version pointers) to prevent it from recurring.
